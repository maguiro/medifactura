# Este archivo es solo de ejemplo — NO lo subas a GitHub
# Railway pone DATABASE_URL automáticamente, no hace falta tocarlo

DATABASE_URL=postgresql://usuario:password@host:5432/medifactura
NODE_ENV=production
PORT=3000
